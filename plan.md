# Plan: prj-4033-task-mugs9k7r

**Project:** `PRJ-2120`  
**Task ID:** `task-mugs9k7r`  
**Repo:** `prj-4033-task-mugs9k7r`  

## Summary

План: создать Telegram-бот для напоминаний на Node.js + grammY, разбит на 5 последовательных subtasks (scaffold → команды/UI → планировщик напоминаний → persistence в SQLite → финальная упаковка и тесты).

## Subtasks

### 1. Scaffold репозитория и базовый бот

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать структуру проекта (package.json, tsconfig, .env.example, src/index.ts), установить grammY + dotenv, реализовать минимальный long-poll бот с командой /start, который отвечает 'Бот работает'. Заложить модульную архитектуру (handlers/, services/, db/, scheduler/).
- **Test plan:** pnpm install без ошибок; BOT_TOKEN из .env.example подхватывается; запуск pnpm dev пишет 'Bot started' в лог; команда /start в Telegram возвращает 'Бот работает'; типы компилируются (tsc --noEmit).

### 2. Команды добавления/списка/удаления напоминаний

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать UX-команды: /add <текст> <время> (например '/add Позвонить маме через 30 минут' или '/add Совещание 2026-10-01 14:30'), /list (показать все активные напоминания пользователя), /delete <id>. Валидация ввода, парсинг времени (относительное и абсолютное), понятные сообщения об ошибках.
- **Test plan:** Ручной smoke-тест в Telegram: /add с корректным временем возвращает подтверждение с id; /add с битым временем возвращает понятную ошибку; /list показывает напоминание; /delete удаляет по id. Unit-тест на парсер времени (минимум 5 кейсов: 'через 5 минут', 'через 2 часа', 'завтра 10:00', '2026-10-01 14:30', невалидная строка).
- **Dependencies:** `sub-1`

### 3. Планировщик и отправка уведомлений

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать in-process scheduler на базе node-cron + setTimeout для точных срабатываний. При наступлении времени — отправка сообщения пользователю через bot.api.sendMessage. Защита от перезапуска: при старте проверять просроченные и сразу помечать отправленными (или отправлять, если прошло <5 мин).
- **Test plan:** Создать напоминание 'через 10 секунд', дождаться — должно прийти сообщение в Telegram; создать 5 напоминаний с разным временем, убедиться что все сработали вовремя (±2 сек). Перезапустить бот — не должно быть дублей уведомлений.
- **Dependencies:** `sub-2`

### 4. Persistence в SQLite

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить better-sqlite3, схема таблицы reminders (id, user_id, text, run_at, status, created_at). Заменить in-memory хранение на БД. Миграция при старте. Защита от двойной отправки: статус pending → sent с атомарным UPDATE.
- **Test plan:** Создать напоминание, рестартовать бот — напоминание остаётся в /list; проверить файл data.db появился; после срабатывания статус в БД = 'sent', повторного сообщения нет. Тест: параллельный scheduler tick не отправляет одно напоминание дважды.
- **Dependencies:** `sub-3`

### 5. README, Docker и финальные тесты

- **ID:** `sub-5`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать README.md с инструкцией запуска, переменными окружения, списком команд. Добавить Dockerfile (multi-stage) + docker-compose.yml. Прогнать весь сценарий: install → lint → typecheck → unit tests → сборка → запуск → smoke-тест команд в Telegram.
- **Test plan:** docker build проходит; docker compose up поднимает бот; README содержит секции Quick start, Commands, Environment, Architecture; CI-скрипт (pnpm test && pnpm build) зелёный; ручной e2e: добавить напоминание 'через 1 минуту' и получить его в Telegram.
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
